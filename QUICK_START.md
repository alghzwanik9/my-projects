# دليل البدء السريع 🚀

## خطوات التشغيل

### 1. تثبيت المكتبات الجديدة

```bash
cd backend
pip install -r requirements.txt
```

**المكتبات الجديدة المطلوبة:**
- `edge-tts` - للصوت الذكوري الطبيعي ✨

### 2. تشغيل السيرفر

```bash
# تأكد أنك في مجلد backend
uvicorn app.main:app --reload --port 8000
```

### 3. فتح الواجهة

افتح المتصفح على: **http://127.0.0.1:8000/**

## اختبار الميزات الجديدة

### اختبار الصوت الذكوري الطبيعي

اكتب نص مثل:
```
الذكاء الاصطناعي هو مستقبل التكنولوجيا
```

ستلاحظ:
- ✅ صوت ذكوري طبيعي (Edge TTS)
- ✅ خلفية تقنية (لون أزرق/رمادي)

### اختبار الخلفيات الديناميكية

جرب نصوص مختلفة:

**تقنية:**
```
الذكاء الاصطناعي والبرمجة والتقنية الحديثة
```

**أعمال:**
```
النجاح في العمل والاستثمار والتجارة
```

**صحة:**
```
الرياضة واللياقة البدنية والصحة الجيدة
```

**تعليم:**
```
التعلم والدراسة والمعرفة والعلوم
```

**تحفيز:**
```
النجاح والطموح والأحلام والتحدي
```

## استكشاف الأخطاء

### إذا لم يعمل Edge TTS:

1. تأكد من تثبيت المكتبة:
```bash
pip install edge-tts
```

2. تأكد من وجود اتصال بالإنترنت (Edge TTS يحتاج الإنترنت)

3. إذا لم يعمل، سيتم استخدام gTTS تلقائياً (fallback)

### إذا لم تعمل الخلفيات:

- النظام سيعود تلقائياً للخلفية الافتراضية
- تأكد من أن `backend/app/services/background_service.py` موجود

## ملاحظات

- ✅ Edge TTS يحتاج اتصال بالإنترنت
- ✅ FFmpeg يجب أن يكون مثبتاً على النظام
- ✅ الصوت الذكوري: `ar-SA-HamedNeural` (صوت سعودي طبيعي)

## جاهز للاستخدام! 🎉

اكتب نصك واختر الفيديو!
