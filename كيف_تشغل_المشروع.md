# 🚀 دليل تشغيل مشروع منشئ الفيديوهات (Shorts Generator)

هذا الدليل سيساعدك على تشغيل المشروع من الصفر وحل المشاكل الشائعة.

---

## 📋 المتطلبات الأساسية

1. **Python 3.11+**: لضمان عمل كافة المكتبات (يفضل الإصدار الذي استقر عليه جهازك).
2. **Node.js & NPM**: لتشغيل واجهة المستخدم (Frontend).
3. **FFmpeg**: ضروري جداً لدمج الصوت والصورة (تأكد من تثبيته في نظامك).

---

## 🛠️ الخطوة 1: تجهيز البيئة (تُنفذ مرة واحدة)

### 1. الباك إند (Backend)
افتح الـ Terminal في المجلد الرئيسي للمشروع ونفذ:
```powershell
# الدخول لمجلد الباك إند
cd backend

# إنشاء بيئة وهمية (تأكد من استخدام مسار بايثون الصحيح)
python -m venv .venv

# تفعيل البيئة (Windows)
.\.venv\Scripts\activate

# تثبيت المكتبات
pip install -r requirements.txt
```

### 2. الفرونت إند (Frontend)
افتح Terminal جديد ونفذ:
```powershell
cd frontend
npm install
```

---

## ▶️ الخطوة 2: تشغيل المشروع (كل مرة تريد العمل)

يجب تشغيل **Terminalين منفصلين**:

### Terminal 1: تشغيل المحرك (Backend) 🧠
```powershell
# تأكد دائماً أنك داخل مجلد backend
cd backend
.\.venv\Scripts\activate
uvicorn app.main:app --reload --port 8000
```
*💡 إذا رأيت `Application startup complete` فهذا يعني أن المحرك يعمل!*

### Terminal 2: تشغيل الواجهة (Frontend) 🎨
```powershell
cd frontend
npm run dev
```
*💡 سيظهر لك رابط مثل `http://localhost:5173` قم بفتحه في المتصفح.*

---

## ⚠️ حلول للمشاكل الشائعة (مهم جداً)

### 1. خطأ "ModuleNotFoundError: No module named 'app'"
**السبب:** أنت تحاول تشغيل الأمر وأنت خارج مجلد `backend`.
**الحل:** تأكد من تنفيذ أمر `cd backend` قبل تشغيل الـ `uvicorn`.

### 2. خطأ "did not find executable at C:\Python314\python.exe"
**السبب:** البيئة الوهمية (`.venv`) مرتبطة بمسار بايثون قديم أو غير موجود.
**الحل:** احذف مجلد `.venv` وأعد إنشاءه باتباع الخطوات في "الخطوة 1" أعلاه.

### 3. خطأ في FFmpeg أو gTTS
تأكد من أن جهازك متصل بالإنترنت (لجلب الأصوات) وأن FFmpeg مضاف لمسار النظام (Path).

---

## ✅ كيف تتأكد أن كل شيء يعمل؟

1. **الباك إند:** افتح [http://127.0.0.1:8000/api/health](http://127.0.0.1:8000/api/health) في المتصفح، يجب أن ترى `{"ok": true}`.
2. **الفرونت إند:** ستظهر لك واجهة احترافية باللون الداكن (Dark Mode) تطلب منك إدخال موضوع الفيديو.

---

## 🎉 بالتوفيق! المشروع الآن جاهز للإبداع. 🎬🚀

