# 🚀 كيف تشغل المشروع - دليل سريع

## 📋 المتطلبات الأساسية

1. **Python 3.8+** - للباك إند
2. **Node.js** - للفرونت إند
3. **FFmpeg** - لإنشاء الفيديوهات

---

## 🔧 خطوة 1: تثبيت المكتبات

### الباك إند (Backend)

```bash
# انتقل لمجلد الباك إند
cd backend

# أنشئ بيئة افتراضية
python -m venv .venv

# فعّل البيئة الافتراضية
# Windows:
.venv\Scripts\activate

# Linux/Mac:
source .venv/bin/activate

# ثبت المكتبات
pip install -r requirements.txt
```

### الفرونت إند (Frontend)

```bash
# انتقل لمجلد الفرونت إند
cd frontend

# ثبت المكتبات
npm install
```

---

## ▶️ خطوة 2: تشغيل المشروع

### الطريقة 1: وضع التطوير (Development) ⭐ الموصى بها

افتح **terminalين منفصلين**:

#### Terminal 1 - الباك إند:
```bash
cd backend
.venv\Scripts\activate  # Windows
# أو
source .venv/bin/activate  # Linux/Mac

uvicorn app.main:app --reload --port 8000
```

سترى رسالة مثل:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
```

#### Terminal 2 - الفرونت إند:
```bash
cd frontend
npm run dev
```

سترى رسالة مثل:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
```

#### افتح المتصفح:
🌐 **http://localhost:5173/**

---

### الطريقة 2: وضع الإنتاج (Production)

#### 1. بناء الفرونت إند:
```bash
cd frontend
npm run build
```

#### 2. تشغيل الباك إند (سيخدم الفرونت إند تلقائياً):
```bash
cd backend
.venv\Scripts\activate  # Windows
uvicorn app.main:app --reload --port 8000
```

#### افتح المتصفح:
🌐 **http://127.0.0.1:8000/**

---

## ✅ التحقق من أن كل شيء يعمل

### 1. تحقق من الباك إند:
افتح: **http://127.0.0.1:8000/api/health**

يجب أن ترى:
```json
{"ok": true, "status": "healthy"}
```

### 2. تحقق من الفرونت إند:
افتح: **http://localhost:5173/** (في وضع التطوير)

يجب أن ترى الواجهة العربية.

---

## 🎬 استخدام التطبيق

1. **اكتب النص** في المربع (10-2000 حرف)
2. **اضغط "🎬 إنشاء الفيديو"**
3. **انتظر** حتى يتم الإنشاء (قد يستغرق 30-60 ثانية)
4. **شاهد الفيديو** و**حمّله**

---

## ⚠️ استكشاف الأخطاء

### مشكلة: "edge-tts not found"
```bash
pip install edge-tts
```

### مشكلة: "FFmpeg not found"
- **Windows**: حمّل من https://ffmpeg.org/download.html
- **Linux**: `sudo apt install ffmpeg`
- **Mac**: `brew install ffmpeg`

### مشكلة: البورت 8000 أو 5173 مستخدم
- غيّر البورت في الأوامر:
  - الباك: `--port 8001`
  - الفرونت: عدّل `vite.config.js`

### مشكلة: CORS errors
- تأكد أن الباك إند يعمل على 8000
- تأكد أن الفرونت إند يعمل على 5173
- Proxy تلقائي في وضع التطوير

---

## 📝 ملاحظات مهمة

- ✅ **Edge TTS** يحتاج اتصال بالإنترنت
- ✅ إذا لم يعمل Edge TTS، سيستخدم **gTTS** تلقائياً
- ✅ النصوص الطويلة تستغرق وقتاً أطول
- ✅ الفيديوهات تُحفظ في `backend/outputs/`

---

## 🎉 جاهز!

الآن يمكنك إنشاء فيديوهات قصيرة احترافية! 🚀
